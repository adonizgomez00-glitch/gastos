# CHECKPOINT — 2026-09-20T16:00:00-06:00

## Metadatos

| Campo | Valor |
|---|---|
| Iteración | ITER-000 |
| Fase | 0 — Descubrimiento y decisiones |
| Disparador | Cierre de fase (manual, solicitado por el usuario) |
| Uso de contexto | ~35 % |
| Spec activa | SPEC-001 (aprobada al cerrar) |
| Referencia de código | `c64a60f` (Árbol limpio. Etiqueta `v0.0.1-fase0`) |

## Qué se hizo

- Relevamiento del entorno (Node 22.23.1, `node:sqlite` sin flag, SQLite 3.50.6, Docker, nginx `:8000`, Tailscale Funnel, puertos ocupados, cron del watchdog de Química, `sudo` con contraseña).
- Nueve decisiones de Fase 0 cerradas con el usuario, una pregunta a la vez y con opciones razonadas.
- Documentación completa: `AGENT.md` (829 líneas, 16 secciones), `ARCHITECTURE.md` (ADR-001..006), `CHECKPOINT.md`, `README.md`, `AGENTS.md` y los 4+1 vivos.
- Contrato anti-contradicción entre `CHECKPOINT.md` y `docs/Context_live.md` (7 reglas) + verificador `npm run check:docs`, **validado con pruebas negativas** (inyectar una afirmación falsa de despliegue o un token de infraestructura produce `FAIL` y salida `1`).
- Generador `npm run context:live` con marcadores de bloque (las notas manuales sobreviven).
- `docs/specs/` con índice, plantilla y SPEC-001 (15 criterios de aceptación Given/When/Then).
- Repositorio git inicializado con 3 commits y etiqueta `v0.0.1-fase0`.

## Estado verificado al cerrar

| Comprobación | Resultado |
|---|---|
| `npm run check:docs` | coherente (0 avisos) |
| `git status` | limpio · `HEAD` = `c64a60f` |
| Código de la aplicación | **ninguno** (solo `.gitkeep` en `server/`, `src/`, `tests/`) |
| Despliegue | no ejecutado (documentado en `docs/DEPLOYMENT.md`) |

## Defectos encontrados y corregidos en esta iteración

| # | Defecto | Fix |
|---|---|---|
| 1 | `gen-context-live.sh` duplicaba el código de salida (`000000`) porque `curl -w` escribe aunque falle | capturar la salida y comprobar si está vacía |
| 2 | La tabla de disco repetía el encabezado de `df` | `awk 'NR>1 && NF>=6'` |
| 3 | La salida de `tailscale funnel status` contiene `|` y rompía la tabla markdown | `tr '|' ' ' | tr -s ' '` |
| 4 | `CHECKPOINT.md` contenía tokens de infraestructura (puerto, `systemctl`, `pkexec`), violando su propia regla | reescrito en términos de proyecto y verificado por `check:docs` (R4) |

> Los defectos 1-3 **no** eran visibles sin ejecutar el generador: se detectaron recién al correrlo.

## Próximo paso (ITER-001)

Implementar **fundaciones + SPEC-001**: config, utilidades, `sqlite.js` con PRAGMA, migración inicial (`users`, `sessions`), router, `PasswordService` (PBKDF2), `AuthService`, `requireAuth`, rutas de auth y health, CLI `migrate`/`create-user`, 15 AC en tests y UI mínima de login.

## Riesgos vigentes

- `node:sqlite` es experimental (aislado en `server/database/sqlite.js`).
- PBKDF2 de 100 k iteraciones haría lenta la suite → iteraciones configurables (1 000 en tests).
- La URL pública es de internet: la autenticación no es opcional.
- Disco `/home` al 86 % (registrado en `docs/Context_live.md`).
