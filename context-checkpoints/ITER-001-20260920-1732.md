# CHECKPOINT — 2026-09-20T23:32:00-06:00

## Metadatos

| Campo | Valor |
|---|---|
| Iteración | ITER-001 (en curso → a cerrar) |
| Fase | 1 — Plan/Implementación: fundaciones + SPEC-001 |
| Disparador | **Manual** (usuario: "checkpoint") |
| Uso de contexto | medio (sesión larga con invariantes documentados) |
| Spec activa | SPEC-001 **aprobada** (en implementación) |
| Referencia de código | Árbol con cambios **sin commitear** (ver `git status` abajo) |

## Qué se hizo desde el último checkpoint (ITER-001-1700)

### Paso 2 completado — suite de 15 AC de SPEC-001

- `tests/run-all.js` (runner propio: descubrimiento, reporte por archivo, exit code real) + harness
  (servidor efímero en puerto 0, BD temporal desechable, reloj inyectable, cliente con cookies).
- 9 archivos de test, **21 en verde, 0 en rojo, ~1.3 s**; los 15 AC cubiertos 1:1.
- Suite verificada el mismo día que se escribió (no se dio por sabido que seguiría en verde).

### Fase C completada — UI mínima de login

| Archivo | Estado |
|---|---|
| `index.html` | creado: etiquetado, meta viewport, cargas relativas, `noscript`, entry point module |
| `assets/css/main.css` | creado: tema oscuro, mobile-first, fuente, paleta |
| `assets/css/components.css` | creado: tarjeta, formulario, botón |
| `assets/favicon.svg` | creado |
| `src/app.js` | creado: router por HASH (hashchange), mount por ruta |
| `src/config/app.js` | creado: rutas como strings (único punto de cambio) |
| `src/services/ApiClient.js` | creado: solo rutas RELATIVAS (ruta absoluta → error) |
| `src/views/LoginView.js`, `HomeView.js`, `html.js` | creados; todo dato del usuario escapado antes de insertarlo |
| `src/controllers/LoginController.js` | creado: submit del formulario, mensaje amigable por código, `/me` para home |
| `server/routes/static.js` | creado: `/`, `/assets/...`, `/src/...` (solo `.js`/`.css`/`.svg`), `..` y `../` rechazados, extensión no `.js` en `/src` rechazada |
| `server/app.js` | actualizado: registra rutas estáticas después de health y auth |

### Verificación en vivo del servidor de desarrollo (127.0.0.1:8100)

| Comprobación | Resultado |
|---|---|
| `GET /` | `200` `text/html` (SPA con estilos + scripts) |
| `GET /assets/css/main.css` | `200` `text/css` |
| `GET /src/app.js` | `200` `application/javascript` |
| `GET /assets/favicon.svg` | `200` |
| `GET /src/repositories/...` (código del servidor) | `404` — **no expuesto** |
| `GET /src/routes/...` | `404` | 
| `POST /api/auth/login` (credenciales inválidas) | `401` |
| `GET /api/health` | `200` |

## Resultados medidos del interruptor de despliegue (package.json)

- Antes: `NODE_ENV=development` con `LOG_LEVEL=info` → `npm start` levantaba la BD sin migraciones
  al arrancar (SQLite creaba la BD en blanco y el servidor fallaba justo al cargar la landing).
- Después: `NODE_ENV=production` / `LOG_LEVEL=warn` por defecto para `npm start`; nuevos scripts
  `npm run dev` (dev + migrar) y `npm run dev:only` (dev sin migrar).
- Riesgo de regresión mantenido como tarea de cuarentena → encerrado en el checkpoint (no se
  deformó); luego se documenta antes de próximo commit (`docs/` borre DCA).

## Estado verificado al checkpoint

| Comprobación | Resultado |
|---|---|
| `npm test` (NODE_ENV=test) | 21 en verde · 0 en rojo · 1.3 s |
| `npm run check:docs` | coherente (0 avisos) |
| `git status` | cambios sin commitear = Fase C (index.html, assets/*, src/*, server/routes/static.js, server/app.js, node_modules) |
| Despliegue real | **no ejecutado** (documentado, pendiente de SPEC-010) |

## Pendientes (en orden)

1. Cerrar ITER-001:
   - SPEC-001 → `🔨 implementada` en README de specs.
   - PSA del interruptor de despliegue (corrección en vivo + riesgo clarificado).
   - güncellemek: `CHECKPOINT.md`, `docs/PROJECT_STATE.md`, `docs/QA_RESULTS.md`, `docs/SESSION.md`.
   - checkpoint final `context-checkpoints/ITER-001-*.md` + commit + etiqueta.
2. Planificar ITER-002 (SPEC-008, tipos de cambio GTQ ⇄ USD).

## Riesgos vigentes

- `node:sqlite` experimental (aislado en `sqlite.js`); recordado en CHECKPOINT §6.
- PBKDF2 a 100 k en producción / 1 k en tests (documentado en config y spec).
- Disco `/home` al 86 % (en `docs/Context_live.md`).
- Interruptor de despliegue acaba de cambiar: riesgo de regresión (cuarentena documentada hasta DCA).

## Protocolo de continuidad

=== CHECKPOINT DE CONTINUIDAD ===
Último checkpoint de contexto: 2026-09-20 17:32 · ITER-001 (a cerrar) · Fase 1.
Estado inmediato: Fase A y B verificadas en vivo; suite 21/21; Fase C realizada y verificada en vivo.
A continuación: cierre de ITER-001 (SPEC-001 → implementada, docs sincronizados, commit + etiqueta).
Próximo paso: PSA del interruptor de despliegue y cierre formal.
=== FIN CHECKPOINT ===
